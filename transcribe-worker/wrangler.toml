name = "reunion-ia-transcribe"
main = "worker.js"
compatibility_date = "2024-01-01"

# Active Workers AI (Whisper hébergé par Cloudflare, gratuit dans la limite
# du quota journalier). Aucune clé API externe n'est nécessaire.
[ai]
binding = "AI"
